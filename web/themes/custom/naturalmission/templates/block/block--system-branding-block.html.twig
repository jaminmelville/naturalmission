<div class='site-heading'>

  {% if site_name %}
    <a href="{{ path('<front>') }}" title="{{ 'Home'|t }}" rel="home">
      <div class="site-heading__title">
        {{ site_name }}
      </div>
    </a>
  {% endif %}

  {% if site_slogan %}
    <div class='site-heading__subtitle'>{{ site_slogan }}</div>
  {% endif %}

</div>
